{
  "name": "site-marlim",
  "version": "2.0.0",
  "engines": {
    "node": "16"
  },
  "scripts": {
    "start": "next start",
    "dev": "next dev",
    "build": "rm -rf build && next build",
    "export": "yarn build && next export",
    "deploy": "yarn export && firebase deploy --only hosting:production"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.0.1",
    "next": "^10.2.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-ga": "^3.3.0",
    "styled-components": "^5.3.0"
  },
  "devDependencies": {
    "babel-plugin-inline-react-svg": "^2.0.1",
    "babel-plugin-styled-components": "^1.12.0"
  },
  "private": true
}
